# 环境安装开发任务清单

> 基于 `spec/60 环境安装设计.md` 对照项目代码的全面审查
> 生成时间: 2026-03-20
> 第三次审查修正: 逐文件验证每一项, 修正误判, 补充遗漏

---

## 使用说明

> **这份清单分为两种口径, 禁止混用**
>
> 1. **严格按 spec 口径**: 只看 `spec/60 环境安装设计.md` 是否已落地, 不接受“体验更好所以不做”的解释
> 2. **项目折中方案口径**: 记录当前代码实际选择的实现方案, 允许与 spec 有意识偏离
>
> 如果任务目标是“检查有没有按照 spec 做”, 必须以 **严格按 spec 口径** 为准。

---

## 结论总览

### 严格按 spec 口径

- ✅ 已完成: Python Embed 初始化、VC++ 检测安装、共享 `python-libs`、镜像测速、`uv`、模型下载重试、`EnvironmentInstallModal` 的多数增强能力
- ❌ 未完成:
  - `EnvironmentGate` 仍使用 `EnvironmentSetupModal`, 未统一到 `EnvironmentInstallModal`
  - `EnvironmentSetupModal` 仍保留, 未按 spec 删除
  - `AgentInstallGuide` 未接入 `EnvironmentInstallModal`
  - `ExtensionsTab` / `ExtensionDetailModal` 仍未统一到 `EnvironmentInstallModal`
  - 运行时检测未完整覆盖 spec 中的 Chromium
  - 模型目录仍存在 `Extensions/<ext>/models` 与 `UserDatas/Models` 并存

### 项目折中方案口径

- 当前代码与这份清单后半部分的“方案调整”基本一致
- 但这些“方案调整”属于**偏离 spec 的实现决策**, 不能写成“已按 spec 完成”

---

## 优先级说明
- 🔴 P0 — 用户体验严重缺失, 需优先修复
- 🟡 P1 — 功能差距, 近期完成
- 🟢 P2 — 改进优化, 可排后

---

## 已确认达标 (无需改动)

| 设计要求                                    | 现状 | 验证证据                                                  |
| ------------------------------------------- | ---- | --------------------------------------------------------- |
| 安装脚本 — 镜像测速                         | ✅    | FunASR L152, QwenTTS L203, demucs ✅                       |
| 安装脚本 — uv 加速                          | ✅    | FunASR L153, QwenTTS L204, demucs ✅                       |
| 安装脚本 — 统一 runPipInstall               | ✅    | FunASR L187/219, QwenTTS L236/268                         |
| 安装脚本 — pip 重试+切镜像                  | ✅    | `runPipInstall` 内置                                      |
| 安装脚本 — Python 自主拉起                  | ✅    | 各安装器自动调用 `installer.install('python')`            |
| 安装脚本 — VC++ 前置检查                    | ✅    | Python 初始化时自动                                       |
| 安装脚本 — 日志记录                         | ✅    | 全部使用 `DebugLogger`                                    |
| 取消功能 — 后端                             | ✅    | `UnifiedInstallService.cancel()` 每个安装器都有 cancel    |
| 取消功能 — 前端按钮                         | ✅    | `InstallingView` L381 有 `取消安装` 按钮                  |
| 下载可靠性 — ExtensionInstaller             | ✅    | GitHub→mirrorFallback, 非GitHub→resume(5)                 |
| 下载可靠性 — AgentEngine/VC++               | ✅    | `downloadWithResume(3)`                                   |
| 下载可靠性 — yt-dlp 更新                    | ✅    | `downloadWithMirrorFallback`                              |
| VocalSeparator 用 EnvironmentInstallModal   | ✅    | L15, L210, L222 onCancel传了cancelInstall                 |
| SpeechToText 用 EnvironmentInstallModal     | ✅    | L9, L423/435/447                                          |
| VideoTranslation 用 EnvironmentInstallModal | ✅    | L18, L778                                                 |
| VideoAnalysis 用 EnvironmentInstallModal    | ✅    | ConfigPanel L6/L389, AnalysisSettingsModal L5/L86         |
| QwenTTS 用 EnvironmentInstallModal          | ✅    | L12, L232, L239 onCancel传了cancel                        |
| getFreeSpace IPC 后端                       | ✅    | types.d.ts L142, preload.ts L453                          |
| installedAt 记录                            | ✅    | ExtensionRegistry.ts L290, LocalApiServiceManager.ts L366 |

---

## 一、EnvironmentInstallModal 增强 (前端)

### 🔴 T1: 确认页 — 磁盘空间检查
- **现状**: 确认页不检查磁盘空间, 不显示剩余
- **后端已有**: `window.electron.getFreeSpace(path)` IPC 已就绪
- **文件**: `src/components/shared/EnvironmentInstallModal.tsx` (`ConfirmView` L168-345)
- **改动**:
  - [x] `ConfirmView` 新增 props: `installPath?: string` (UserDatas 路径)
  - [x] `useEffect` 调用 `window.electron.getFreeSpace(installPath)` 获取可用空间
  - [x] 从 `EngineVariant.size` 或 `config.modelSize` 解析所需空间 (需解析 "约 800 MB" → 数值)
  - [x] 渲染: `💾 需要 ~X GB / 磁盘可用 Y GB ✅` 或 `⚠️ 空间可能不足`
  - [x] 切换 variant 时重新计算 (listen selectedVariant)
  - [x] 空间不足时安装按钮不禁用, 但有黄色警告

### 🟡 T2: 安装中 — 下载速度和耗时统计
- **现状**: 进度条只有 `{pct}% · 预计约 5-10 分钟`, 是静态提示
- **已有后端**: 部分安装器在 progress.message 中包含速度信息
- **文件**: `src/components/shared/EnvironmentInstallModal.tsx` (`InstallingView` L350-385)
- **改动**:
  - [x] progress 类型扩展: `{ percent, message, speed?: string }`
  - [x] `InstallingView` 记录 `startTime = Date.now()` (useRef)
  - [x] 计算已用: `formatDuration(Date.now() - startTime)`
  - [x] 估算剩余: `remainMs = elapsed * (100 - pct) / pct`
  - [x] 渲染: `⬇ 12.3 MB/s · 已用 3:25 · 约剩 ~5 分钟` (替换静态 installTimeHint)
  - [x] 无 speed 时退化: 仅显示百分比 + 已用时间

### 🟡 T3: 安装中 — 可折叠安装日志
- **现状**: `InstallingView` 无日志区域, 用户看不到 pip/下载详情
- **文件**: `src/components/shared/EnvironmentInstallModal.tsx` (`InstallingView`)
- **改动**:
  - [x] 父组件 `EnvironmentInstallModal` 新增 `logs: string[]` 状态
  - [x] progress 回调每次 push message 到 logs (限 500 行)
  - [x] `InstallingView` 接收 `logs` props
  - [x] 渲染折叠组件: `📋 安装日志 (n 行)`, 点击展开
  - [x] 展开区域: `<pre>` + 暗色背景 + 等宽字体 + maxHeight 200px + overflow-y scroll
  - [x] 自动滚底: `useEffect` + `scrollTop = scrollHeight`

### 🟡 T4: 结果页 — 失败日志和复制按钮
- **现状**: `ResultView` L390-419 失败时只显示 `result.error` 一行
- **文件**: `src/components/shared/EnvironmentInstallModal.tsx` (`ResultView`)
- **改动**:
  - [x] `ResultView` 接收 `logs?: string[]` props
  - [x] 失败时显示 logs 最后 20 行 (默认展开)
  - [x] `[📋 复制日志]` 按钮 → `navigator.clipboard.writeText()`
  - [x] 复制成功: 文字变 `已复制 ✓`, 2 秒恢复

---

## 二、取消安装 — 各页面一致性修复 (前端)

### 🟡 T5: onCancel 回调不一致
- **现状**: 6 个页面的 `onCancel` 行为不统一
  - ✅ VocalSeparator: `cancelInstall()` + 关弹窗 (正确)
  - ✅ Qwen3TTSLocalPanel: `cancelInstall()` + 关弹窗 (正确)
  - ❌ VideoTranslation L785: 只关弹窗, 不取消安装
  - ❌ VideoAnalysis/ConfigPanel L396: 只关弹窗, 不取消安装
  - ❌ SpeechToText: 完全没传 `onCancel`
- **改动**:
  - [x] VideoTranslation `onCancel` → 调用实际取消安装 IPC + 关弹窗
  - [x] ConfigPanel `onCancel` → 同上
  - [x] SpeechToText 补传 `onCancel` 到 3 个 EnvironmentInstallModal 实例

---

## 三、下载可靠性 (后端)

### 🟡 T6: demucs 模型下载 — 改用镜像轮询
- **现状**: L443 用 `makeGithubUrl()` + `downloadWithResume` (单源断点续传)
- **应改为**: `downloadWithMirrorFallback(原始GithubURL)` (多镜像测速轮询)
- **文件**: `electron/services/vocalSeparator/separators/demucsInstaller.ts` L440-458
- **改动**:
  - [x] `makeGithubUrl` + `downloadWithResume` → `downloadWithMirrorFallback`
  - [x] 进度回调格式适配

### 🟡 T7: FunASR 模型下载 — 加 TS 层重试
- **现状**: Python `snapshot_download` 无重试
- **文件**: `electron/services/asr/providers/FunASRInstaller.ts`
- **改动**:
  - [x] `preDownloadModel()` 包装重试: 3 次, 指数退避
  - [x] 重试时发 progress: `下载失败, Xs 后重试...`

### 🟡 T8: QwenTTS 模型下载 — 加 TS 层重试
- **文件**: `electron/services/tts/QwenTTSInstaller.ts`
- **改动**: 同 T7



### 🟢 T10: 提取公共重试函数
- **文件**: 新建 `electron/services/utils/downloadRetry.ts`
- **改动**:
  - [x] 提取 `retryPythonDownload(fn, maxRetries, onProgress)` 公共函数
  - [x] T7/T8 改为调用公共函数

---

## 四、清理旧安装逻辑 (前端重构)

> ⚠️ 本节必须区分:
> - **严格按 spec**: T11/T12/T13/T14 都应继续推进
> - **当前折中方案**: 项目选择保留旧入口或局部自包含安装
>
> 下方的“方案调整/取消”仅代表**当前项目选择**, 不代表“已符合 spec”

### ⚠️ T11: EnvironmentSetupModal 增强 (当前折中方案)
- **实际方案**: 不替换 modal，而是增强现有 `EnvironmentSetupModal`
- **调整原因**: SetupModal 处理多依赖门控，InstallModal 面向单引擎，两者互补不是替代
- **文件**: `src/components/environment/EnvironmentSetupModal.tsx`
- **改动**:
  - [x] 新增折叠式安装日志 (500行缓存 + 自动滚底)
  - [x] 新增安装计时器 (底部显示已用时间)
  - [x] 失败时显示最后 20 行日志 + 复制按钮
- **严格按 spec 结论**: 这不是最终态。spec 要求统一到 `EnvironmentInstallModal`，这里只是给旧弹窗补功能。

### 🔴 T12: 删除 EnvironmentSetupModal (严格按 spec 仍未完成)
- **spec 要求**: 删除 `src/components/environment/EnvironmentSetupModal.tsx`
- **当前状态**: 未删除，且 `EnvironmentGate` 仍在实际调用
- **代码证据**:
  - `EnvironmentGate.tsx` 仍 `import { EnvironmentSetupModal }`
  - 缺失依赖时仍直接渲染 `<EnvironmentSetupModal />`
- **当前折中方案**: 暂时保留，不等于 spec 已完成

### 🔴 T13: 改造 AgentInstallGuide
- **严格按 spec 要求**: 改为自动安装流程，并集成 `EnvironmentInstallModal`
- **当前折中方案**: 不套 `EnvironmentInstallModal`，而是自包含一键安装
- **实际方案**: 不套 EnvironmentInstallModal，而是自包含一键安装
- **调整原因**: CLI 工具安装不需要 variant/model 概念，已有 `agent-manager:install` IPC
- **文件**: `src/features/agent-expert/components/AgentInstallGuide.tsx`
- **改动**:
  - [x] 新增「一键安装」按钮 → 调用 `agent-manager:install` IPC
  - [x] 实时显示安装日志 (监听 `agent-manager:install-log`)
  - [x] 安装成功/失败状态处理
  - [x] 失败时回退显示手动命令
- **严格按 spec 结论**: 未完成。当前实现只是“自动安装能力已补”，不是“统一入口已完成”。

### 🔴 T14: 拓展中心安装统一
- **严格按 spec 要求**: `ExtensionsTab` / `ExtensionDetailModal` 安装入口统一走 `EnvironmentInstallModal`
- **当前状态**: 仍走 `useExtensions.install()` 和 `ExtensionDetailModal`
- **当前折中方案**: 保留卡片内联进度与详情弹窗安装
- **结论**: 按 spec 口径仍未完成

### 🔴 T11 补充: EnvironmentGate 适配 (严格按 spec 仍未完成)
- **spec 要求**: `EnvironmentGate` 缺失依赖时弹 `EnvironmentInstallModal`
- **当前状态**: 仍弹 `EnvironmentSetupModal`
- **文件**: `src/components/environment/EnvironmentGate.tsx`
- **结论**: 这是“统一安装入口”里最核心的未完成项之一

---

## 五、被动安装覆盖缺口 (前端)

### 🟡 T15: 视频分割器 — EngineSelector 需安装引擎无法安装
- **现状**: `EngineSelector.tsx` L67-70 未安装引擎直接 `disabled`, 不弹安装
- **设计要求**: 选中未安装引擎 → 弹 `EnvironmentInstallModal`
- **文件**: `src/features/toolbox/video-splitter/components/EngineSelector.tsx`
- **改动**:
  - [x] 点击未安装引擎 → 不是 disabled, 而是触发安装弹窗
  - [x] 新增 `onNeedInstall(engineId)` 回调
  - [x] 父组件响应: 弹 `EnvironmentInstallModal with SCENE_DETECTION_CONFIG`
  - [x] 需新增 `SCENE_DETECTION_INSTALL_CONFIG` 预设

---

## 六、UI 改进 (前端)

### 🟢 T16: 拓展中心卡片 — 显示占用空间
- **文件**: `ExtensionCard.tsx`
- **改动**:
  - [x] 后端新增: IPC `ext:get-size` → 递归计算目录大小
  - [x] 前端渲染 `v1.0.0 · 已用 2.5 GB`

### 🟢 T17: 详情弹窗 — 显示安装时间和空间
- **后端已有**: `installedAt` 字段在 ExtensionRegistry / LocalApiServiceManager 中记录
- **文件**: `ExtensionDetailModal.tsx`
- **改动**:
  - [x] 基本信息区: `安装时间: 2026-03-15 | 占用空间: 2.5 GB`

---

## 任务优先级排序

| 优先级 | 任务                       | 工作量   | 说明                  |
| ------ | -------------------------- | -------- | --------------------- |
| 🔴 P0   | T1 磁盘空间检查            | 0.5 天   | 后端 IPC 已有, 纯前端 |
| 🟡 P1   | T5 onCancel 不一致修复     | 1 小时   | 3 个文件各补一行回调  |
| 🟡 P1   | T2 下载速度显示            | 0.5 天   |                       |
| 🟡 P1   | T3 安装日志折叠            | 0.5 天   |                       |
| 🟡 P1   | T4 失败日志+复制           | 0.5 天   |                       |
| 🟡 P1   | T6 demucs 镜像轮询         | 0.5 小时 | 换一个函数调用        |
| 🟡 P1   | T10 公共重试函数           | 0.5 小时 |                       |
| 🟡 P1   | T7-T9 模型下载重试         | 0.5 天   | 3 个文件              |
| 🟡 P1   | T11 EnvironmentGate 适配   | 1 天     | 严格按 spec 必做      |
| 🟡 P1   | T12 删除旧 SetupModal      | 10 分钟  | 严格按 spec 必做      |
| 🟡 P1   | T13 AgentInstallGuide 改造 | 1 天     | 严格按 spec 仍未完成  |
| 🟡 P1   | T14 拓展中心统一           | 2 天     | 严格按 spec 仍未完成  |
| 🟡 P1   | T15 视频分割器安装入口     | 0.5 天   |                       |
| 🟢 P2   | T16-T17 UI 增强            | 1 天     |                       |

**总计约: 9 个工作日**

---

## 建议执行顺序

```
第 1 批 — EnvironmentInstallModal 增强 + 快速修复 (3 天):
  T1 磁盘空间检查 (P0)
  T5 onCancel 一致性修复 (快, 1小时)
  T2 下载速度
  T3 安装日志
  T4 失败日志

第 2 批 — 下载可靠性 (1 天):
  T10 公共重试函数
  T6 demucs 改镜像轮询
  T7-T9 模型下载重试

第 3 批 — 统一安装入口 (2.5 天):
  T11 EnvironmentGate 适配
  T12 删旧 SetupModal (T11后)
  T13 AgentInstallGuide 改造
  T15 视频分割器安装入口

第 4 批 — 拓展中心 + UI 增强 (3 天):
  T14 拓展中心统一安装弹窗
  T16-T17 UI 增强
```

---

## 本次审查修正记录

| 轮次            | 修正内容                                                                    |
| --------------- | --------------------------------------------------------------------------- |
| 第一次 → 第二次 | ❌ T14-T15 误判 FunASR/Qwen 没有镜像测速/uv → 实际已有, 删除                 |
| 第一次 → 第二次 | ➕ 新增 T5 取消安装按钮                                                      |
| 第二次 → 第三次 | ❌ T5 误判"前端无取消按钮" → InstallingView L381 已有, 改为"onCancel 不一致" |
| 第二次 → 第三次 | ➕ 新增 T15 视频分割器 EngineSelector 安装入口缺失                           |
| 第二次 → 第三次 | 📝 T5 拆细: 3 个文件的具体 onCancel 问题逐行标注                             |
| 第二次 → 第三次 | 📝 所有已达标项列出具体代码行号作为证据                                      |
| 第三次 → 第四次 | ⚠️ 明确区分“严格按 spec”与“当前折中方案”，避免将方案变更写成 spec 已完成     |
